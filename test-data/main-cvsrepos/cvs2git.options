# (Be in -*- mode: python; coding: utf-8 -*- mode.)

# An options file to test converting to git.

# Actually, the example file cvs2git-example.options is fully
# functional, so we just load it:
execfile('cvs2git-example.options')

